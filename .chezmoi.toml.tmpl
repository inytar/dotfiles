{{- $user := promptStringOnce . "user" "Local Username" -}}
{{- $email := promptStringOnce . "email" "Email address" -}}
[data]
    email = "{{ $email }}"
    user = "{{ $user }}"
    nerdFonts = [ "Meslo" ]
[edit]
    command = "vim"
[diff]
    exclude = ["scripts", "externals"]
